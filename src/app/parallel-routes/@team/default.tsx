import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function DefaultTeam() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Team Overview</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Select a specific team view from the sidebar.</p>
      </CardContent>
    </Card>
  )
}

